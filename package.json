{
  "name": "harness-conda",
  "version": "0.6.0",
  "description": "Create, reproduce, and switch isolated Agent environments.",
  "type": "module",
  "bin": {
    "harness": "dist/src/cli.js",
    "harness-conda": "dist/src/cli.js"
  },
  "files": [
    "dist/src",
    "docs",
    "examples/performance-engineering",
    "examples/reproducibility-core",
    "examples/paper-search",
    "examples/idea-gen",
    "examples/exp-design",
    "examples/auto-research",
    "scripts/benchmark-switch.mjs",
    "CHANGELOG.md",
    "README.md",
    "SECURITY.md"
  ],
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean --silent && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/*.test.js",
    "benchmark": "npm run build --silent && node scripts/benchmark-switch.mjs",
    "start": "node dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "agent",
    "skills",
    "mcp",
    "codex",
    "claude-code",
    "harness",
    "environment",
    "package-manager",
    "reproducibility"
  ],
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zedong-peng/harness-conda.git"
  },
  "homepage": "https://github.com/zedong-peng/harness-conda#readme",
  "bugs": {
    "url": "https://github.com/zedong-peng/harness-conda/issues"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "semver": "^7.8.5",
    "smol-toml": "^1.4.2",
    "yaml": "^2.8.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/semver": "^7.7.1",
    "typescript": "^5.8.0"
  }
}
