apiVersion: harness.conda/v1
kind: Harness
metadata:
  name: harness-project-memory
  version: 0.2.0
  description: Load and update project-specific knowledge when a user explicitly requests it.
  tags: [memory, project-context]
spec:
  platforms: [codex, claude, pi, qoder]
  entrypoints:
    - name: manage-project-memory
      skill: harness-project-memory
      description: Load or update project knowledge on explicit user request.
  skills:
    - name: harness-project-memory
      path: ./skills/harness-project-memory
